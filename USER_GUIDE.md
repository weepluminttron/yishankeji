# 买家版说明书（USER_GUIDE.md）

欢迎使用 AI 获客系统。本文档按“第一次使用 → 日常使用 → 进阶技巧”的顺序，帮你快速上手。

---

## 1. 第一次使用（5 分钟配置）

1. 部署完成（见 DEPLOY.md）后，浏览器打开 `http://你的服务器IP:8017`；
2. 输入 `.env` 里设置的 `ACCESS_PASSWORD` 登录；
3. 进入左侧 **设置**，确认以下配置已生效（这些也可以直接在 `.env` 里改）：
   - AI 接口：已填 DeepSeek 密钥则显示“AI 已就绪”；
   - 搜索接口：选好搜索源（博查/SerpAPI/免费源），可点“检测搜索源”看哪些可用；
   - 地图接口：已填高德 Key 则地图渠道自动启用；
   - 公司信息：填上你的公司名、主营产品，后续 AI 生成内容都会用到。

> 建议先导入演示数据熟悉系统：在“客户获取 → 批量导入”里上传 `examples/sample_leads.csv` 或 `examples/sample_leads.json`（均为虚构数据，用完后请删除）。

---

## 2. 三分钟快速上手（核心流程）

整个系统就三步：

### 第一步：AI 获客（找客户）

进入左侧 **AI 获客**（或工作台中央大输入框）：

1. 用一句话描述你的业务和理想客户，例如：
   > “我公司做光纤跳线和预端接光缆，想找有数据中心项目的系统集成商和弱电工程商。”
2. 点击“开始 AI 获客”；
3. 弹窗第一步：选择 **你是哪种角色**（贸易商/分销商、代理商、生产厂家、系统集成商、外贸出口商等）；
4. 弹窗第二步：勾选 **客户在哪些地区**（可多选，不选表示不限）；
5. 系统自动整理关键词并直接开始找客户，进度看右上角任务栏；
6. 完成后自动显示客户清单（公司、区域、买方类型、命中规格、等级、建议动作），可一键 **导入客户库**。

### 第二步：客户管理（筛客户）

进入 **客户线索**：

- 列表按评分排序，S/A/B/C 级一眼看清；
- 点开客户详情：联系人、电话、邮箱、跟进记录、备注、下次提醒；
- 可批量改状态、加标签、导出 Excel；
- “全量 AI 评分”可让 AI 给所有线索重新打分，并给出跟进建议。

### 第三步：主动触达（跟客户）

进入 **主动触达**：

- 选客户 → 选模板（邮件/短信）→ 系统自动替换 {{公司名}} {{联系人}} 等变量；
- 配置 SMTP 后可真正发邮件；也可以复制话术后去微信/电话手动跟进；
- 线索详情里点“AI 跟进”，按客户画像一键生成个性化首触邮件或开场白。

---

## 3. 功能详解

### 3.1 工作台（首页）
- 顶部高频操作：AI 获客、买家发现、地图获客、批量导入、客户列表、AI 评分、邮件触达、跟进序列；
- 数据漏斗：新线索/待联系/已联系/跟进中/已成交数量；
- 最新线索：可直接发邮件、加备注、查看详情；
- 右上角任务栏：显示正在运行的任务（AI 生成方案、获客引擎、评分等）及其状态。

### 3.2 买家发现（手动搜索）
适合想自己控制关键词时使用：输入行业关键词 × 目标地区，系统搜索潜在买家并抓取官网联系方式，勾选后入库。

### 3.3 地图获客
在“地图获客”里输入关键词（如“弱电工程”“系统集成”“光纤”）和城市，直接从高德地图拉取公司名、电话、地址，非常适合找本地工程商。

### 3.4 获客落地页
访问 `http://你的服务器IP:8017/lp` 会打开一个“免费获取报价”表单页，访客留资后自动进入客户库。文案、电话、按钮都可在设置里改。

### 3.5 数据清洗
客户多了以后，用“数据清洗”自动找出重复客户（同公司名/同电话），一键合并。

### 3.6 获客分析
查看转化漏斗、各来源转化率、各意向阶段转化率、高意向待跟进清单，指导你把精力放在最可能成交的客户上。

---

## 4. 常见问题与技巧

**Q1：为什么客户很少？**
- 免费搜索源容易被限流，建议配置博查/SerpAPI（推荐博查，价格便宜）；
- 地图渠道是稳定的保底来源，务必配置高德 Key 并多换几个关键词（“弱电工程”“系统集成”“网络工程”“安防监控”）；
- 检查右上角任务栏或结果区顶部的提示，看是哪个渠道被限流；
- “检索种子词”里加采购信号词：光纤 采购 / 光纤 招标 / 光纤 询价；
- 地区别选太窄，先选“中国大陆”或“亚太”跑一轮。

**Q2：搜索提示被限流/403？**
- 到 设置 → 搜索接口 → 点“检测搜索源”看哪些源可用；
- 启用代理：.env 里 PROXY_ENABLED=1，填 PROXY_URL（代理 IP）或 PROXY_API_URL（短效动态IP接口）；
- 免费源限流一般 10 分钟自动恢复，先做别的，稍后再试。

**Q3：AI 方案不显示/一直转圈？**
- 确认 AI 密钥已配置且余额充足；
- 生成中进度在右上角任务栏，完成后会自动跳回结果页；
- 如果提示超时，重新点一次“开始 AI 获客”。

**Q4：导入的线索为什么说重复？**
系统按“公司名/电话”自动去重，同一客户不会重复入库，重复提示是正常保护。

**Q5：怎么把客户数据带走？**
客户列表支持一键导出 Excel/CSV；整个 `data/` 目录就是全部数据，备份它即可。

**Q6：我可以修改界面上的文案吗？**
可以。前端文件在 `static/`，后台“设置”里也能改公司名、产品名、落地页文案。改代码前建议先备份。

---

## 5. 合规提醒

- 请只使用公开渠道的信息做合法商业联络，不要购买/使用泄露的个人手机号库；
- 群发邮件请使用企业邮箱并控制发送频率，避免被判定垃圾邮件；
- 系统仅供合法获客使用，请勿用于骚扰、诈骗或侵犯他人隐私。
