# 安全策略

## 报告漏洞

如果你发现安全漏洞，请**不要**直接公开提 Issue，以免被滥用。

请通过以下方式私信反馈（请自行替换为你的真实联系方式）：

- 邮箱：security@example.com （占位，待维护者填写）
- 或在 GitHub 上私信仓库维护者

我们会在收到后尽快确认并跟进修复，修复发布后再公开致谢（如你愿意）。

## 已知使用注意事项

本工具为销售获客用途，公网部署时请注意以下事项：

1. **访问密码**：务必在 `server.env` 中设置强密码的 `ACCESS_PASSWORD`，不要使用 `deploy/yishankeji.service` 里的默认占位密码。
2. **抓取 / 买家发现接口**：网页采集、买家发现等功能的 URL 由管理员在后台输入，服务端会代为发起网络请求。公网部署时建议在受信任的网络环境使用，避免让不可信用户输入任意地址；当前版本未在代码层对目标地址做网络隔离（如拦截内网 / 云元数据地址），请在部署网络层面自行做好隔离。
3. **密钥与凭证**：SMTP 密码、AI 接口 Key、群机器人 Webhook 等敏感信息仅保存在本地 `data/` 与 `server.env`，均已加入 `.gitignore`，请勿提交到仓库。
4. **数据备份**：线索库位于 `data/app.db`，请定期备份 `data/` 目录。

## 安全更新

安全相关修复会随版本提交进入 `main` 分支，建议关注仓库更新。
